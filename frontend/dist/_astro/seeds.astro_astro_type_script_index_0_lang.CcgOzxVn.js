const r="http://localhost:8000/api",h=document.getElementById("error-msg");function a(n){h.textContent=n,h.classList.remove("hidden")}function i(){h.classList.add("hidden")}async function m(n){try{const t=await n.json();return Array.isArray(t.detail)?t.detail[0]?.msg||"Request failed":t.detail||"Request failed"}catch{return"Request failed"}}async function c(){i();const t=await(await fetch(`${r}/seeds`)).json(),s=document.getElementById("seeds-list");if(!t.length){s.innerHTML='<div class="alert bg-base-200 text-sm">No seeds yet. Click "Generate from interests" or "Generate from sources" to create some.</div>';return}s.innerHTML=`<ul class="list bg-base-100 border border-base-300 rounded-box divide-y divide-base-200">
        ${t.map(e=>{const l=e.source_url?(()=>{try{return new URL(e.source_url).hostname}catch{return e.source_url}})():null;return`
          <li class="list-row items-center gap-3 py-3">
            <div class="min-w-0 flex-1 max-w-[75%]">
              <div class="font-medium">${o(e.title)}</div>
              <div class="text-xs text-base-content/40 mb-1">${o(e.source_name||"")}</div>
              ${e.source_url?`<div class="flex items-center gap-1 text-sm text-base-content/50">
                    <a href="${o(e.source_url)}" target="_blank" rel="noopener noreferrer" class="truncate max-w-[200px] hover:text-primary transition-colors" title="${o(e.source_url)}">${o(l)}</a>
                    <button class="btn btn-square btn-ghost btn-xs" data-action="copy-url" data-url="${o(e.source_url)}" aria-label="Copy URL"><svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="9" y="9" width="13" height="13" rx="2" ry="2"/><path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"/></svg></button>
                  </div>`:""}
              <div class="flex flex-wrap gap-1 mt-1">
                ${(e.tags||[]).map(u=>`<span class="badge badge-sm badge-soft badge-primary">${o(u)}</span>`).join("")}
              </div>
            </div>
            <div class="flex items-center gap-2 shrink-0 ml-auto">
              <span class="badge badge-md badge-soft whitespace-nowrap ${e.status==="pending"?"badge-warning":e.status==="approved"?"badge-info":e.status==="used"?"badge-success":"badge-error"}">${e.status}</span>
              <span class="text-sm text-base-content/40 whitespace-nowrap">${e.created_at?new Date(e.created_at).toLocaleDateString():"—"}</span>
              <div class="flex gap-1">${e.status==="pending"?`<button class="btn btn-square btn-ghost btn-sm text-info" data-action="approve" data-id="${e.id}" aria-label="Approve seed"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg></button><button class="btn btn-square btn-ghost btn-sm text-error" data-action="discard" data-id="${e.id}" aria-label="Discard seed"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M18 6 6 18"/><path d="m6 6 12 12"/></svg></button>`:'<button class="btn btn-square btn-ghost btn-sm btn-disabled" tabindex="-1" aria-disabled="true" aria-label="Approve seed"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg></button><button class="btn btn-square btn-ghost btn-sm btn-disabled" tabindex="-1" aria-disabled="true" aria-label="Discard seed"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M18 6 6 18"/><path d="m6 6 12 12"/></svg></button>'}</div>
            </div>
          </li>`}).join("")}
      </ul>`}function o(n){if(!n)return"";const t=document.createElement("div");return t.appendChild(document.createTextNode(n)),t.innerHTML}document.getElementById("seeds-list").addEventListener("click",async n=>{const t=n.target.closest("[data-action]");if(!t)return;i();const s=t.dataset.id;try{if(t.dataset.action==="discard"){const e=await fetch(`${r}/seeds/${s}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({status:"discarded"})});if(!e.ok)throw new Error(await m(e));c()}else if(t.dataset.action==="approve"){const e=await fetch(`${r}/seeds/${s}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({status:"approved"})});if(!e.ok)throw new Error(await m(e));c()}else if(t.dataset.action==="copy-url")try{await navigator.clipboard.writeText(t.dataset.url);const e=t.innerHTML;t.innerHTML='<svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="20 6 9 17 4 12"/></svg>',t.classList.add("text-success"),setTimeout(()=>{t.innerHTML=e,t.classList.remove("text-success")},1500)}catch{a("Failed to copy URL")}}catch(e){a("Error: "+e.message)}});document.getElementById("btn-generate-interests").addEventListener("click",()=>{i(),document.getElementById("interests-input").value="",document.getElementById("interests-modal").showModal()});document.getElementById("interests-cancel").addEventListener("click",()=>{document.getElementById("interests-modal").close()});document.getElementById("interests-generate").addEventListener("click",async()=>{const n=document.getElementById("interests-input").value.trim();if(!n){a("Please enter at least one interest keyword.");return}const t=n.split(",").map(e=>e.trim().toLowerCase()).filter(Boolean);if(!t.length){a("Please enter at least one interest keyword.");return}const s=document.getElementById("interests-generate");s.disabled=!0,s.textContent="Generating...",i();try{const u=(await(await fetch(`${r}/sources`)).json()).filter(d=>d.enabled!==!1&&(t.some(g=>d.name.toLowerCase().includes(g))||(d.tags||[]).some(g=>t.some(y=>g.toLowerCase().includes(y))))).map(d=>d.id);if(!u.length){a("No sources match your interests. Add sources with matching tags first."),s.disabled=!1,s.textContent="Generate";return}const b=await fetch(`${r}/seeds/generate`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({source_ids:u})});if(!b.ok)throw new Error(await m(b));document.getElementById("interests-modal").close(),c()}catch(e){a("Error: "+e.message)}finally{s.disabled=!1,s.textContent="Generate"}});async function f(){try{const t=await(await fetch(`${r}/sources`)).json(),s=document.getElementById("sources-list-modal");if(!t.length){s.innerHTML='<div class="text-sm text-base-content/40">No sources configured.</div>';return}s.innerHTML=t.map(e=>`
          <label class="flex items-center gap-3 p-2 rounded-box hover:bg-base-200 cursor-pointer ${e.enabled===!1?"opacity-40":""}">
            <input type="checkbox" class="checkbox checkbox-sm source-checkbox" data-id="${o(e.id)}" ${e.enabled!==!1?"":"disabled"} />
            <div class="min-w-0 flex-1">
              <div class="text-sm font-medium">${o(e.name)}</div>
              <div class="text-xs text-base-content/40">${o(e.source_type)} ${e.tags?.length?"— "+e.tags.map(l=>o(l)).join(", "):""}</div>
            </div>
          </label>
        `).join(""),s.querySelectorAll(".source-checkbox").forEach(e=>{e.addEventListener("change",p)}),p()}catch{document.getElementById("sources-list-modal").innerHTML='<div class="text-sm text-error">Failed to load sources.</div>'}}function p(){const n=document.querySelectorAll(".source-checkbox:checked");document.getElementById("sources-generate").disabled=n.length===0}document.getElementById("btn-generate-sources").addEventListener("click",()=>{i(),document.getElementById("sources-list-modal").innerHTML='<span class="loading loading-spinner loading-sm"></span>',f(),document.getElementById("sources-modal").showModal()});document.getElementById("sources-cancel").addEventListener("click",()=>{document.getElementById("sources-modal").close()});document.getElementById("sources-generate").addEventListener("click",async()=>{const n=document.querySelectorAll(".source-checkbox:checked"),t=Array.from(n).map(e=>e.dataset.id);if(!t.length){a("Select at least one source.");return}const s=document.getElementById("sources-generate");s.disabled=!0,s.textContent="Generating...",i();try{const e=await fetch(`${r}/seeds/generate`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({source_ids:t})});if(!e.ok)throw new Error(await m(e));document.getElementById("sources-modal").close(),c()}catch(e){a("Error: "+e.message)}finally{s.disabled=!1,s.textContent="Generate"}});c();
